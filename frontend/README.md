# CampusOps Frontend

Vite React + TypeScript UI for CampusOps AI.

Run from the project root:

```bash
npm run dev
```

Run only the frontend:

```bash
npm --prefix frontend run dev
```

The Vite server proxies `/api` requests to the local backend at `http://127.0.0.1:4174`.
